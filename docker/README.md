# docker/

`Dockerfile` (multi-stage: dashboard build → slim Python runtime) and
`compose.yaml` (server + optional MongoDB backend) arrive in Phase 0/2.
Docker is a convenience, never a requirement.
